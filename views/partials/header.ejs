<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Site de fou</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
        <link rel="stylesheet" href="style_header.css">
    </head>
    <body>
      <div class="container">
        <!-- Header en flexbox -->
        <header class="d-flex align-items-center justify-content-between py-3 mb-4 border-bottom">
          
          <!-- Colonne pour la barre de navigation -->
          <div class="flex-grow-1">
            <ul class="nav justify-content-center justify-content-md-start mb-2 mb-md-0">
              <li class="nav-item">
                <a href="/" class="nav-link px-2 link-secondary">
                  <button class="button" data-text="Awesome">
                    <span class="actual-text">&nbsp;Home&nbsp;</span>
                    <span aria-hidden="true" class="hover-text">&nbsp;Home&nbsp;</span>
                  </button>
                </a>
              </li>
              <li class="nav-item"><a href="/gamelist" class="nav-link px-3">Liste jeux</a></li>
              <li class="nav-item"><a href="/dashboard" class="nav-link px-3">Dashboard</a></li>
              <li class="nav-item"><a href="/quizz" class="nav-link px-3">Quizz</a></li>
              <li class="nav-item"><a href="#" class="nav-link px-3">About</a></li>
            </ul>
          </div>
        
          <!-- Colonne pour le login/sign-up ou username -->
          <div class="text-end">
            <% if (username) { %>
              <h3 class="mb-0"><%= username %></h3>
            <% } else { %>
              <a href="/login"><button type="button" class="btn btn-outline-primary me-2">Login</button></a>
              <a href="/register"><button type="button" class="btn btn-primary">Sign-up</button></a>
            <% } %>
          </div>
    
        </header>
      </div>
            <div class="container" style="padding-top: 10px;" >